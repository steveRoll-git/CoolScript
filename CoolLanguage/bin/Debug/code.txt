var other = 999;

var multiply = function(x){
	print("alright, let's go")
	print("multiplying " .. x .. "... it's " .. x * 2)
	return x * 2
}

var num = tonumber(input("what number "))

print("mult is " .. multiply(num))